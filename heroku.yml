setup:
  config:
    REACT_APP_SESSION_ENCRYPTION_KEY: testcase
    REACT_APP_GOOGLE_MAPS_KEY: ${REACT_APP_GOOGLE_MAPS_KEY}
    REACT_APP_STRIPE_API_TEST_KEY: ${REACT_APP_STRIPE_API_TEST_KEY}
build:
  docker:
    web: Dockerfile
