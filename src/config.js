/*
* Configuration file that makes environment variables accessible within JS through process.env
* */

import dotenv from "dotenv"

// todo: not loading Heroku's config-vars
// execute dotenv configuration
dotenv.config()
