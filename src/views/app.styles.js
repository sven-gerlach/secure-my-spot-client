import styled from "styled-components";

const Div = styled("div")`
  height: 100%;
  width: 100%;
  // relative position setting ensures that this div is the containing block for the Alert component
  position: relative;
`

export default Div
